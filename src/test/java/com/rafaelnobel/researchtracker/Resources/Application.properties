# --- DATABASE TEST (H2 MEMORY) ---
# Ini database "mainan" yang hidup di RAM cuma pas testing
# Supaya test tidak error gara-gara koneksi PostgreSQL
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=posgres
spring.datasource.password=NobelMar07120
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Matikan log yang tidak perlu biar terminal bersih
spring.jpa.show-sql=false
spring.main.banner-mode=off